import React from 'react';
import {Text, View} from 'react-native';

const MessageSettings = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
        backgroundColor: 'white',
      }}>
      <Text>Hello, MessageSettings!😛</Text>
    </View>
  );
};
export default MessageSettings;
