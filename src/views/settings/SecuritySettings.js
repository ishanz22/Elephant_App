import React from 'react';
import {Text, View} from 'react-native';

const SecuritySettings = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
        backgroundColor: 'white',
      }}>
      <Text>Hello, Security!</Text>
    </View>
  );
};
export default SecuritySettings;
